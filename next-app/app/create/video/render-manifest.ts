const aspects=["9:16","4:5","1:1","16:9"];
export function validateRenderManifest(b:any){const sc=Array.isArray(b?.scenes)?b.scenes:[],a=Array.isArray(b?.assets)?b.assets:[];if(!sc.length||!a.length)throw Error("RENDER_MANIFEST_EMPTY");if(sc.length!==a.length)throw Error("SCENE_ASSET_MISMATCH");const aspect=aspects.includes(b.aspect)?b.aspect:"9:16";return{durationMs:15000,aspect,assets:a.map((x:any)=>({url:String(x.url||""),mimeType:String(x.mimeType||"image/png")})),scenes:sc.map((x:any)=>({durationMs:Math.max(500,Math.min(15000,Number(x.durationMs||3000))),motion:String(x.motion||"zoom-in"),transition:String(x.transition||"fade")})),overlays:Array.isArray(b.overlays)?b.overlays.map((o:any)=>({type:String(o.type||"caption"),text:String(o.text||""),startMs:Number(o.startMs||0),endMs:Number(o.endMs||15000),position:["top","center","bottom"].includes(o.position)?o.position:"center",fontSize:Number(o.fontSize||48),opacity:Number(o.opacity??1)})).filter((o:any)=>o.text):[]}}
