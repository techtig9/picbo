import {AppShell} from "@/components/app-shell";import {createClient} from "@/lib/supabase/server";import {getCurrentWorkspace} from "@/lib/auth";import {createProduct} from "./actions";import Link from "next/link";
export default async function Products(){const ws=await getCurrentWorkspace();const supabase=await createClient();const {data:products}=ws?.workspace_id?await supabase.from("products").select("id,name,sku,brand,created_at").eq("workspace_id",ws.workspace_id).order("created_at",{ascending:false}):{data:[]};return <AppShell><div className="content"><div className="eyebrow">PRODUCT INTELLIGENCE</div><h1 className="title">Product Library</h1><p className="muted">Every generation starts from a controlled product source.</p><div className="grid three" style={{marginTop:22}}><div className="card"><h3>Add a product</h3><form action={async (formData:FormData)=>{"use server";await createProduct(formData);}} className="form"><label>Name<input name="name" placeholder="e.g. Noir Perfume" required/></label><label>SKU<input name="sku" placeholder="Optional SKU"/></label><label>Brand<input name="brand" placeholder="Optional brand"/></label><label>Description<textarea name="description" style={{minHeight:80}} placeholder="Product details…"/></label><button className="btn primary">Create product</button></form></div><div className="card"><h3>Product Identity</h3><p className="muted">Reference images, packaging, logo, text, shape, colors and proportions become a reusable identity layer.</p><div className="notice">Identity analysis will be connected to the AI provider router in Phase 5.</div></div><div className="card"><h3>Brand Kit</h3><p className="muted">Keep logos, colors, typography, voice and campaign rules reusable across every output.</p><Link href="/brand-kits" className="btn">Manage brand kits</Link></div></div><div className="head"><h2>Products</h2><span className="muted">{products?.length||0} products</span></div><div className="card"><table className="table"><thead><tr><th>Name</th><th>Brand</th><th>SKU</th><th>Created</th></tr></thead><tbody>{(products||[]).map((p:any)=><tr key={p.id}><td>{p.name}</td><td>{p.brand||"—"}</td><td>{p.sku||"—"}</td><td>{new Date(p.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div></AppShell>}
