import Link from "next/link";import {signUp} from "../actions";
export default async function SignUp({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="auth"><div className="auth-card"><div className="logo">P</div><h1>Create your Picbo.ai account</h1><p className="muted">Start creating product content and campaigns.</p>{q.error&&<div className="notice error">{q.error}</div>}<form action={signUp} className="form"><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" minLength={8} required autoComplete="new-password"/></label><button className="btn primary" type="submit">Create account</button></form><p className="muted">Already have an account? <Link href="/auth/sign-in">Sign in</Link></p></div></main>}
