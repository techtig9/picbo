import {NextResponse} from "next/server";import {createVideoProject} from "@/app/create/video/actions";
export async function POST(req:Request){try{const b=await req.json();if(!b.productId)return NextResponse.json({error:"productId is required"},{status:400});const duration=Number(b.durationSeconds||15);if(duration!==15&&duration!==30)return NextResponse.json({error:"Supported durations are 15 or 30 seconds"},{status:400});const r=await createVideoProject({...b,durationSeconds:duration,aspectRatio:b.aspectRatio||"9:16",captions:b.captions!==false});return NextResponse.json(r)}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==="UNAUTHENTICATED"?401:500})}}
