import {NextResponse} from "next/server";import {createAdBrief} from "@/app/create/ads/actions";
export async function POST(req:Request){try{const b=await req.json();if(!b.productId)return NextResponse.json({error:"productId is required"},{status:400});if(!Array.isArray(b.platforms)||!b.platforms.length)return NextResponse.json({error:"Choose at least one platform"},{status:400});const r=await createAdBrief({...b,variants:Math.min(20,Math.max(1,Number(b.variants||4)))});return NextResponse.json(r)}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==="UNAUTHENTICATED"?401:500})}}
