import {AppShell} from "@/components/app-shell";import {createClient} from "@/lib/supabase/server";import {getCurrentWorkspace} from "@/lib/auth";
export default async function Assets(){const ws=await getCurrentWorkspace();const supabase=await createClient();const {data:assets}=ws?.workspace_id?await supabase.from("assets").select("id,kind,status,mime_type,created_at").eq("workspace_id",ws.workspace_id).order("created_at",{ascending:false}):{data:[]};return <AppShell><div className="content"><div className="eyebrow">MEDIA MANAGEMENT</div><h1 className="title">Asset Library</h1><p className="muted">Central storage for product references, generated creatives, videos, audio and campaign exports.</p><div className="hero"><h2>Asset pipeline</h2><p className="muted">Private storage + workspace permissions + metadata + product/project links.</p><div className="chips"><span className="chip">Private bucket</span><span className="chip">Signed URLs</span><span className="chip">Workspace isolated</span><span className="chip">Metadata</span></div></div><div className="head"><h2>Assets</h2><span className="muted">{assets?.length||0} assets</span></div><div className="card"><table className="table"><thead><tr><th>Type</th><th>Status</th><th>MIME</th><th>Created</th></tr></thead><tbody>{(assets||[]).map((a:any)=><tr key={a.id}><td>{a.kind}</td><td><span className="status">{a.status}</span></td><td>{a.mime_type||"—"}</td><td>{new Date(a.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div></AppShell>}
