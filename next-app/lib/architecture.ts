export type TaskKind="text"|"image"|"video"|"render"|"import"|"publish"|"analytics";export type QualityTier="fast"|"balanced"|"pro";export interface GenerationRequest{task:TaskKind;quality:QualityTier;productId?:string;prompt?:string;aspectRatio?:string;durationSeconds?:number}export interface ProviderResult{provider:string;model:string;requestId:string;costUsd?:number;assetIds?:string[];output?:unknown}export interface AIProvider{supports(task:TaskKind):boolean;generate(request:GenerationRequest):Promise<ProviderResult>}export const ROUTER_RULES={fallback:"Only configured providers/models; never bypass quotas."} as const;
